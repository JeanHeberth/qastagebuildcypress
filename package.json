{
  "name": "qastagebuild",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test": "echo \"Error: no test specified\" && exit 1",
    "browser:chrome": "npx cypress run --browser=chrome --headed --spec cypress/e2e --reporter mocha-allure-reporter",
    "report:allure": "npx allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cypress/grep": "^4.1.0",
    "@faker-js/faker": "^8.4.1",
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "cypress": "^13.13.0",
    "gerador-validador-cpf": "^5.0.2"
  },
  "dependencies": {
    "allure-cypress": "^3.0.0-beta.6",
    "express": "^4.19.2",
    "faker": "^5.5.3",
    "mocha-allure-reporter": "^1.4.0"
  }
}
